---
description: Audit current commitments and force keep/change/kill verdicts with reclaimed capacity allocated to existing work.
argument-hint: [the list of current commitments, or "use what I told you earlier"]
allowed-tools: Read, Write
---

You are running the `kill-list` subskill of the `outcome-first-decisions` skill.

## Commitments to audit

$ARGUMENTS

## What to do

1. Load `subskills/kill-list/SKILL.md` and follow it exactly.
2. Apply the parent skill's Default Posture and Self-Check Protocol from `SKILL.md`.
3. Use the Kill List Audit framework in `references/frameworks-core.md`.
4. Use `templates/kill-list.md` for the audit table format.
5. Reference `references/anti-patterns.md` for Sunk-Cost Loyalty, Multi-Goal Drift, Roadmap Theater, Calendar Gravity, and Endless Maybe.

## Hard requirements

The output must include:

- A verdict on every item — keep, change, or kill. No maybes.
- The drop-candidate triggers checked for each killed item.
- A captured learning for every kill (original thesis, why it died, what to reuse).
- Reclaimed capacity quantified (hours/week or $/month).
- An explicit allocation of that capacity to existing kept commitments — not to new work.
- Three actions the user can execute today.
- A next-audit date (default: 90 days).

## Hard rules

- Force a verdict on every item. Maybes are kills in disguise.
- Never let reclaimed capacity drift to new commitments invented during the audit.
- Always capture a learning before killing.
- Refuse to mark an item "keep" without a named buyer, scoreboard contribution, and next proof test.

## Calibration sample

Read `examples/kill-audit.md` once before responding to match voice and depth.
