interface:
  display_name: "Outcome-First Decisions"
  short_description: "Validate ideas, prioritize work, force kill/keep decisions"
  default_prompt: "Use $outcome-first-decisions to validate this business idea, design a 7-day proof test, and name a kill criterion in writing."

policy:
  allow_implicit_invocation: true

attribution:
  author: "Thorsten Meyer"
