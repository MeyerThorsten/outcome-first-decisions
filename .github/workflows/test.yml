name: Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate package structure
        run: python3 tests/validate_package.py
      - name: Verify zip builds
        run: ./build/package.sh
